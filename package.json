{
  "name": "defigo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --project .",
    "build:docker": "docker build --tag chdefrene/defigo-ha-shim --platform linux/amd64 .",
    "start": "node dist/app"
  },
  "dependencies": {
    "body-parser": "^1",
    "express": "^4",
    "node-fetch": "^2"
  },
  "devDependencies": {
    "@types/body-parser": "^1",
    "@types/express": "^4",
    "@types/node-fetch": "^2",
    "prettier": "^2",
    "typescript": "^5"
  }
}
